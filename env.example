# Copy this file to .env and fill in your key. .env is only read locally
# (via python-dotenv) and should never be committed to source control.

ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the model used for AI insights.
# ANTHROPIC_MODEL=claude-sonnet-5
